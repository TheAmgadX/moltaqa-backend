apiVersion: v1
kind: Service
metadata:
  name: postgres-user-service
  namespace: default
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app: postgres-user
